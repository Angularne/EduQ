{
  "name": "kosystem",
  "version": "1.0.0",
  "description": "kosystem",
  "main": "server/build/server.js",
  "scripts": {
    "install": "cd server && npm install && typings install && cd ../client && npm install && typings install",
    "start": "nodemon ./server/build/server.js",
    "build": "ntsc -p server && ntsc -p client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Angularne",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Angularne/kosystem.git"
  }
}
